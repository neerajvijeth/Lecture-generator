# ════════════════════════════════════════════════════════════════════════════
# requirements.txt — single venv for app.py + streamlit_app.py + Manim
# ════════════════════════════════════════════════════════════════════════════
#
# SYSTEM DEPENDENCIES (install these first — not pip):
#
#   macOS:
#     brew install cairo pango pkg-config ffmpeg poppler
#     # LaTeX (for Manim MathTex):
#     brew install --cask mactex-no-gui        # or full mactex (~4 GB)
#
#   Ubuntu/Debian:
#     apt-get install libcairo2-dev libpango1.0-dev pkg-config \
#                     ffmpeg poppler-utils texlive-latex-base \
#                     texlive-fonts-recommended texlive-latex-extra
#
#   After system deps, create and activate ONE venv:
#     python3 -m venv venv
#     source venv/bin/activate          # macOS/Linux
#     venv\Scripts\activate             # Windows
#     pip install -r requirements.txt
#
# ════════════════════════════════════════════════════════════════════════════

# ── Core AI ─────────────────────────────────────────────────────────────────
google-genai>=0.8.0

# ── Document processing ──────────────────────────────────────────────────────
PyPDF2>=3.0.0
python-pptx>=0.6.21
python-docx>=1.1.0

# ── Text-to-speech ───────────────────────────────────────────────────────────
gTTS>=2.3.2

# ── Image + PDF processing ───────────────────────────────────────────────────
Pillow>=10.4.0
pdf2image>=1.16.3
requests>=2.31.0

# ── Manim (same venv — replaces separate manim_env) ─────────────────────────
manim>=0.18.1

# ── Web UI ───────────────────────────────────────────────────────────────────
streamlit>=1.40.0

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
numpy>=1.26.0
edge-tts